# serverless-appsync-basic standalone
Please change the profile name and region name in serverless.yml
change the account number in config.\<env\>.json

To run the script:
- npm install
- npm run deploy
  - npm run deploy --stage \<arg\> --region \<region\>

